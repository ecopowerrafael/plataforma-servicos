import type { ReactNode } from 'react';
export function SubscriptionOverviewCard({ children }: { children: ReactNode }) { return <section className="platform-panel platform-subscription-overview"><h4>Visão geral do contrato</h4>{children}</section>; }
