import { IconCloud, IconQrcode } from '@tabler/icons-react';

export function ConnectionTypeSelector({ selected, active, onSelect, onCompare }: { selected: 'WAPI' | 'META'; active: 'WAPI' | 'META'; onSelect: (value: 'WAPI' | 'META') => void; onCompare: () => void }) {
  const options = [{ id: 'WAPI' as const, title: 'API não oficial', subtitle: 'Conexão rápida escaneando o QR Code do WhatsApp Web.', icon: IconQrcode }, { id: 'META' as const, title: 'Meta Cloud API (Oficial)', subtitle: 'Recomendada para volume, estabilidade e templates oficiais.', icon: IconCloud }];
  return <section className="whatsapp-type-selector"><div className="whatsapp-section-heading"><h2>Escolha o método de conexão</h2><p>Você pode trocar de método a qualquer momento.</p></div><div className="whatsapp-provider-grid whatsapp-provider-grid--compact">{options.map(({ id, title, subtitle, icon: Icon }) => <button key={id} type="button" className={`whatsapp-provider-option whatsapp-provider-option--compact ${selected === id ? 'is-selected' : ''}`} onClick={() => onSelect(id)}><span className="whatsapp-provider-icon"><Icon size={24} /></span><span className="whatsapp-provider-option__title"><strong>{title}</strong><small>{subtitle}</small></span>{active === id ? <span className="whatsapp-status-badge is-primary">EM USO</span> : id === 'META' ? <span className="whatsapp-status-badge is-success">RECOMENDADO</span> : null}</button>)}</div><button className="text-button whatsapp-compare-link" type="button" onClick={onCompare}>❓ Dúvida sobre qual escolher? Comparar métodos</button></section>;
}
