import { IconCheck, IconLock } from '@tabler/icons-react';
export function IncludedFeaturesList({ limits }: { limits: any[] }) { const features = limits.filter(x => x.valueType === 'BOOLEAN'); return <section className="billing-section"><div className="billing-section-heading"><div><span className="billing-kicker">SEU PLANO</span><h3>Recursos incluídos</h3></div></div><div className="features-grid">{features.map(item => <div className={`feature-item ${item.booleanValue ? '' : 'is-disabled'}`} key={item.key}>{item.booleanValue ? <IconCheck size={17} /> : <IconLock size={16} />}<span>{item.key.split('.')[0].replaceAll('_', ' ')}</span></div>)}</div></section>; }
