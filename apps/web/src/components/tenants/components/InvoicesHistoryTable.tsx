export function InvoicesHistoryTable({ charge, onPay }: { charge: any; onPay: () => void }) { return <section className="billing-section"><div className="billing-section-heading"><div><span className="billing-kicker">FINANCEIRO</span><h3>Histórico de cobranças</h3></div></div>{charge ? <div className="invoice-table-wrap"><table className="invoice-table"><thead><tr><th>Data</th><th>Valor</th><th>Método</th><th>Status</th><th /></tr></thead><tbody><tr><td>{new Date(charge.createdAt).toLocaleDateString('pt-BR')}</td><td>{(Number(charge.amountCents) / 100).toLocaleString('pt-BR', { style: 'currency', currency: charge.currency || 'BRL' })}</td><td>{charge.provider === 'pix-local' ? 'PIX direto' : 'Mercado Pago'}</td><td><span className={`invoice-status invoice-status--${charge.status.toLowerCase()}`}>{charge.status === 'PAID' ? 'Pago' : 'Pendente'}</span></td><td>{charge.status !== 'PAID' && <button type="button" onClick={onPay}>Pagar</button>}</td></tr></tbody></table></div> : <p className="billing-empty">Nenhuma cobrança registrada ainda.</p>}</section>; }
