export function PlanUpgradeCards({ plans, currentId, onSelect }: { plans: any[]; currentId: string; onSelect: (id: string) => void }) { return <section className="billing-section"><div className="billing-section-heading"><div><span className="billing-kicker">ESCOLHA O PRÓXIMO PASSO</span><h3>Planos disponíveis</h3></div></div><div className="plans-grid">{plans.map(plan => { const current = plan.publicId === currentId; return <article className={`plan-card ${current ? 'is-current' : ''}`} key={plan.publicId}>{current && <span className="plan-card__badge">PLANO ATUAL</span>}<h4>{plan.name}</h4><p>{plan.shortDescription ?? plan.description}</p><strong>{(Number(plan.priceCents) / 100).toLocaleString('pt-BR', { style: 'currency', currency: plan.currency || 'BRL' })}</strong><small>/ período</small>{!current && <button type="button" onClick={() => onSelect(plan.publicId)}>Comparar e mudar</button>}</article>; })}</div></section>; }
