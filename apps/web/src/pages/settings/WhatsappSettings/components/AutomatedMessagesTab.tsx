import { IconChevronDown, IconClock } from '@tabler/icons-react';
const cards = [['confirmation', 'Confirmação de agendamento', 'Enviada imediatamente ao confirmar um novo horário.'], ['dayBefore', 'Lembrete do dia anterior', 'Ajude o cliente a se preparar para o atendimento.'], ['minutesBefore', 'Lembrete antes do atendimento', 'Reduza atrasos com um lembrete no momento certo.'], ['cancellation', 'Cancelamento de agendamento', 'Confirme quando um agendamento for cancelado.']];
export function AutomatedMessagesTab({ canManage }: { canManage: boolean }) { return <div className="wa-tab-content"><section className="wa-card"><div className="wa-section-heading"><div><span className="wa-kicker">AUTOMAÇÕES</span><h2>Mensagens que trabalham por você</h2><p>Escolha quando cada cliente deve receber uma atualização.</p></div></div><div className="wa-accordion-list">{cards.map(([id, title, description], index) => <details className="wa-accordion" key={id} open={index === 0}><summary><span className={`wa-automation-icon tone-${index}`}><IconClock size={18} /></span><span><strong>{title}</strong><small>{description}</small></span><input type="checkbox" defaultChecked disabled={!canManage} onClick={(e) => e.stopPropagation()} /><IconChevronDown className="wa-chevron" size={18} /></summary><div className="wa-accordion-body"><label>Mensagem automática</label><textarea disabled={!canManage} defaultValue={index === 0 ? 'Seu agendamento na {tenantName} foi confirmado!' : index === 1 ? 'Lembrete: você tem um agendamento amanhã na {tenantName}.' : index === 2 ? 'Seu atendimento na {tenantName} começa em {time} minutos.' : 'Seu agendamento foi cancelado com sucesso.'} /><div className="wa-automation-options">{index === 1 && <label>Horário de envio <input type="time" defaultValue="18:00" disabled={!canManage} /></label>}{index === 2 && <label>Enviar <input type="number" defaultValue="30" min="5" disabled={!canManage} /> minutos antes</label>}</div></div></details>)}</div></section></div>; }
