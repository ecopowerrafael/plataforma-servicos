import { IconAlertCircle, IconDeviceFloppy } from '@tabler/icons-react';
export function UnsavedChangesBar({ onDiscard, onSave, saving, disabled }: { onDiscard: () => void; onSave: () => void; saving?: boolean; disabled?: boolean }) { return <div className="wa-save-bar"><IconAlertCircle size={18} /><span>Você possui alterações não salvas.</span><button type="button" onClick={onDiscard}>Descartar</button><button className="primary" type="button" disabled={disabled || saving} onClick={onSave}><IconDeviceFloppy size={16} />{saving ? 'Salvando…' : 'Salvar alterações'}</button></div>; }
